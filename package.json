{
  "scripts": {
    "client": "cd client && npm run dev",
    "server": "uvicorn main:app --reload",
    "env:fedora": "source server/env/bin/activate",
    "dev": "concurrently \"npm run client\" \"npm run server\""
  },
  "devDependencies": {
    "concurrently": "^10.0.0"
  }
}
